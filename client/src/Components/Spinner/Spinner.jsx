// Spinner component
import "../Spinner/Spinner.css"; // importing the css for the spinners components
const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
